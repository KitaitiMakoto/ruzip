default:
  image: ruby:3.4.7

  before_script:
    - apt-get update && apt-get install -y clang
    - curl https://sh.rustup.rs -sSf | sh -s -- -y
    - . "$HOME/.cargo/env"
    - gem update --system
    - gem install bundler
    - bundle install

test:
  script:
    - bundle exec rake test
